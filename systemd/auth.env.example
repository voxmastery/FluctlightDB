# Optional API credentials for Fluctlight serve and agent clients.
# Copy to /etc/fluctlight/auth.env (mode 600). Never commit real secrets.

FLUCTLIGHT_API_KEY=change-me
# FLUCTLIGHT_API_KEYS=default:change-me:write,readonly:other-key:read
