#!/usr/bin/env bash
# Strip Cursor attribution trailers from commit messages (Agent adds these by default).
sed -i \
  -e '/^Co-authored-by: Cursor <cursoragent@cursor.com>$/d' \
  -e '/^Co-authored-by: Cursor$/d' \
  -e '/^Made-with: Cursor$/d' \
  -e '/^Made with Cursor$/d' \
  "$1"
