#!/usr/bin/env bash
# Remove automated tool attribution trailers from commit messages.
sed -i \
  -e '/cursoragent@/d' \
  -e '/^Made-with:/d' \
  -e '/^Co-Authored-By:/d' \
  -e '/^Claude-Session:/d' \
  "$1"
