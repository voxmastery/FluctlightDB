# GitHub labels (maintainers)

Create once on the repo if missing:

```bash
gh label create reproduction --color "1D76DB" --description "Independent benchmark reproduction report"
gh label create benchmark --color "FBCA04" --description "Benchmark harness / eval work"
```

Used by `.github/ISSUE_TEMPLATE/reproduction.yml`.
